apiVersion: v1
kind: Service
metadata:
  name: {{ include "osrm-backend.fullname" . }}
  labels:
    {{- include "osrm-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "osrm-backend.selectorLabels" . | nindent 4 }}
